
server:
  port: 8083
  
endpoints:
  sensitive: true
  health:
    sensitive: true

management:
  security:
    enabled: false

spring:
  boot:
    admin:
      url: http://localhost:8081
  application:
    name: Invoice
    
